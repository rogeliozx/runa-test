/* eslint-disable react/jsx-filename-extension */
import React from 'react';
import Trainer from '../views/Trainer';

export default function TrainerLayout({ history }) {
  return <Trainer history={history} />;
}
